import { expect, type Page } from '@playwright/test';

export default class SignInPage {
    readonly page: Page;

    constructor(page: Page) {
        this.page = page;
    }

    async test() {
        
    }

}